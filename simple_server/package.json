{
  "name": "simple_server",
  "version": "1.0.0",
  "description": "This is a simple server which can be dockerized",
  "main": "app.js",
  "scripts": {
    "start": "node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "debarshi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
